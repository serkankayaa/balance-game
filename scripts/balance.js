$(document).ready(function () {
    var balances = [];
    var game = game1;

    prepareBalances(game);

    function prepareBalances(game) {
        const balanceCount = game.leftChamber.length;
        const balanceHtml = "";

        for (let i = 0; i < balanceCount; i++) {
        }
    }
});
